struct Perfil {
    nombre:&str,
    pais:&str
}

fn crear(n:&str, p:&str) -> Perfil {
    let pf = Perfil { nombre:n, pais:p };
    return pf;
}

fn main() {
    let user = crear("flavia", "peru");
    println!("{}", user.nombre);
    println!("{}", user.pais);
}
