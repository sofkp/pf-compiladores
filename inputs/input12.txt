static mut g: i32 = 2;

fn inc(v: i32) -> i32 {
    return v + 1;
}

fn main() {
    let mut x: i32 = 0;

    while x < 3 {
        println!(inc(x));
        x = x + 1;
    }

    if g > 1 {
        println!(100);
    } else {
        println!(200);
    }
}